# Login-shell environment — river/way-displays need these before any rc runs.
# Actual options live in ~/.config/shell/. Managed by stow (ArchDotfiles).

source ~/.config/shell/env.sh
source ~/.config/shell/path.sh
