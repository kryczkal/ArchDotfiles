# .zprofile: login-shell loader. Managed by stow (ArchDotfiles).
# Only `source` lines. Options live in ~/.config/shell/. river, way-displays
# and anything spawned from the TTY see exactly env.sh + path.sh.

source ~/.config/shell/env.sh
source ~/.config/shell/path.sh
