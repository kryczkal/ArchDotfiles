# aliases.sh: plain aliases only. Managed by stow (ArchDotfiles).
# Not here: functions (put a script in ~/.local/bin instead), exports (env.sh).

alias ls="lsd"
alias l="lsd -lah"
alias cat="bat -p"

alias paru='paru --color=always --bottomup'
