-- Options are automatically loaded before lazy.nvim startup
-- Default options that are always set: https://github.com/LazyVim/LazyVim/blob/main/lua/lazyvim/config/options.lua
-- Add any additional options here
--
---- Disable relative line numbers (use standard 1, 2, 3...)
vim.opt.relativenumber = false

-- Remove the empty space at the bottom where the status bar used to be
-- 0 = never show status line
vim.opt.laststatus = 0

-- Optional: If you find the command line (where you type :w) too distracting,
-- you can set cmdheight to 0, so it only pops up when you type.
vim.opt.cmdheight = 0
