# Always. pacman. Rule: every line has a reason. Add a package only when
# something you run does not run without it, and say what.
base linux linux-firmware sudo networkmanager openssh   # boots, connects, sudo, ssh
base-devel git stow                                     # install itself, paru builds
sof-firmware                                            # audio firmware, Raptor Lake needs it
zram-generator e2fsprogs dosfstools                     # swap, fsck for ext4 root + vfat EFI
zsh zsh-autosuggestions zsh-syntax-highlighting         # shell
neovim tmux man-db                                      # editor, multiplexer, man
ripgrep fd curl unzip                                   # for: nvim (LazyVim pickers, Mason downloads)
bat lsd                                                 # for: aliases.sh (cat, ls)
pipewire pipewire-pulse wireplumber                     # audio
bluez bluez-utils                                       # bluetooth, bluetoothctl to pair
tlp                                                     # battery
xdg-utils                                               # xdg-open
